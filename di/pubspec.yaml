name: di
description: Di module
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  app:
    path: ../
  domain:
    path: ../domain
  data:
    path: ../data


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
